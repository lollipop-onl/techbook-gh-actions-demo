name: ESLintチェック
on: push

jobs:
  eslint:
    name: ESLintチェック
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - run: yarn install
      - run: yarn run lintjs
